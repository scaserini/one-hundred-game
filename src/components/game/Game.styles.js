import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  gameContainer: {
    height: 'calc(100% - 16px)',
    padding: '8px',
    position: 'relative',
  },
});
